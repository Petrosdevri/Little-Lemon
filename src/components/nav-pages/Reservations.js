export default function Reservations() {
    return (
        <header className="reserve-table">
            <div className="reserve-header-text">
                <h1>Login</h1>
            </div>
        </header>
    );
}